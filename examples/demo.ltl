// examples/demo.ltl — runnable demo for ltl_seccomp_demo

import std::io
use ltl_seccomp_demo as lib

fn main() {
    io::println("ltl_seccomp_demo demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}